Sphinx build task.

example:default settings

default setting details
    buildCommand sphinx-build
    target       html
    documentRoot document
    output       build/document

task buildDocument(type : Sphinx) {
}

example:override settings
    target       html->singlehtml
    documentRoot document->doc
    output       build/document->output/doc

task buildDocument(type : Sphinx) {
    target 'singlehtml'
    documentRoot 'doc'
    output  'output/doc'
}

